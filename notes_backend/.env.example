# Port to run the FastAPI app on (default: 3001)
PORT=3001

# Comma-separated list of allowed CORS origins. Use "*" for all (development).
# Example: http://localhost:5173,http://localhost:3000
CORS_ALLOW_ORIGINS=*

# Use integer IDs instead of UUIDs for notes (true/false). Default: false (UUIDs)
NOTES_USE_INT_IDS=false
